# Handoff — M12 closed

- Exact product: `c06386e9344c07d86ea4a3b63dc73f37f20ceb0e`
- Product tree: `4b963c89be0c2709077e3da4076adf0e122d3fe7`
- Remote branch: `codex/integration/m12` points exactly to the product
- Automatic evidence:
  [run `31143886273/1`](https://github.com/zzffu/ferrum2/actions/runs/31143886273)
- Manual performance/resource evidence:
  [run `31144255549/1`, job `92760357350`](https://github.com/zzffu/ferrum2/actions/runs/31144255549)
- State: M12-T01～T06 done；M12 closed

## Completed

- Pinned Hickory 0.26.1 and raised workspace/CI MSRV to Rust 1.88.0 while retaining build toolchain
  1.97.1。
- Added one optional schema-v1 tagged DNS graph：client UDP/TCP proxy inbounds、server domain
  resolution through UDP/TCP/DoT/DoH and per-server direct or existing-action detour。
- Reused one core first-match matcher with separate outbound/DNS actions；numeric bootstrap、no fallback、
  fixed admission/buffers/deadlines and awaited owners remain enforced。
- Kept normal/default/release trust WebPKI-only；the reviewed test CA is available only through the
  default-off isolated interop feature。

## Verified

- Exact local product passed T01～T05 focused evidence、format、strict Clippy、workspace binary build/
  all-features tests、Rust 1.88 check/build/test、ignored 100+ lifecycle、docs and diff。
- Automatic run `31143886273/1` passed quality、schema 3 footprint、MSRV、Windows MSVC、Linux GNU/musl、
  SIP022 TCP/UDP `12/12` each plus cleanup、all 12 CoreDNS/BIND DNS cases and aggregate qualification。
- Manual run `31144255549/1` completed all nine jobs successfully。Five exact completion markers report
  throughput medians `137553510/478270805`、ratio `0.287605910`、10 trials；10,000 sessions、180 samples、
  RSS `6/6` and drain PASS；DNS direct `4564`、detoured `4450` queries、48 samples、RSS `12/12`、bounds/
  drain/rebind PASS。Performance cleanup succeeded；the ratio remains diagnostic only。
- Schema 3 reports code/tests `18940/39748`、ratio `2.098627` and case/support/fixture growth
  `6211/1081/0`。Integrity/change pass；numeric `REVIEW_REQUIRED` is accepted as independent DNS
  transport、negative、lifecycle、interop and resource evidence，with no fixture or second harness/data plane。

## Root cause and repair

Manual run `31134561696/1` on `17f412be0195b6bc7cd2be7944b64d808442f66f` failed the detoured-DNS
server owner ceiling and remains visible。Local `/proc` evidence showed FDs `16 -> 219` in about one
second while tasks stayed at `18`：each query dropped its client `PreparedClientUdp`，but the server
correctly retained that SIP022 UDP session until idle expiry。

Commit `c06386e9` reuses an idle association only for the same static server and exact concrete hop plan，
returns it only after successful relay and otherwise drops it。The existing UDP manager still owns the
capacity ceiling；no config、protocol、dependency or detached-task surface was added。Sequential reuse、
selector snapshot、saturation/error cleanup and exact process rebind evidence pass。

## Boundaries and next action

- Failed evidence is not reclassified。Each changed exact SHA received at most one authorized push and
  manual dispatch；no unchanged-SHA rerun or duplicate dispatch occurred。
- Post-repair architecture and QA audits are `PASS` with zero blocking finding。Deferred DNS cache、
  DNSSEC、DoQ/DoH3、server groups/retry/fallback and advanced matchers remain outside M12。
- No M12 action remains。The closeout commit containing this file is documentation-only and is not a new
  qualified product SHA。No PR、tag、package、release or publication was performed。
