# Handoff — M11 validating

- Exact product: `6d975c1e45eb0e614c54961e35fdc19fa2478d98`
- Product tree: `2d022daccf06c31b0e7106bb1427559c9999c79b`
- Automatic hosted evidence:
  [run `30943770483/1`](https://github.com/zzffu/ferrum2/actions/runs/30943770483/attempts/1)
- State: M11-T01/T02/T03/T04 done；M11-T05 blocked；M11 validating
- Remote boundary: the authorized one non-force push is consumed；manual performance is not authorized

## Completed

- T01 compiled additive per-outbound credentials and immutable bounded direct/chain plans；T02/T03
  composed ordered TCP/UDP chains through existing SIP022 seams；T04 proved mixed-credential two-hop
  TCP/UDP with real processes。All ticket blocking findings are closed。
- T05 completed focused、serial Full、Rust 1.85、100+ lifecycle、footprint and blocking reviews on the
  exact product，then used the one authorized non-force push for automatic hosted qualification。
- Automatic attempt 1 passed quality、test-footprint、MSRV、three native platforms、TCP/UDP interop
  and final qualification。Performance was correctly skipped because it runs only on
  `workflow_dispatch`。

## Verified

- Public selector `3/3`、config `14/14`、CLI `5/5`；four composition rows and both T04
  real-process rows `1/1` each；architecture `9/9` and Rust 1.85 all-target passed。
- Serial format、strict Clippy、workspace binaries/all-features tests、docs、diff and footprint gates
  exited `0`；the ignored 100+ lifecycle row passed `1/1` in `131.07s`。
- Run `30943770483/1` succeeded on exact SHA
  `6d975c1e45eb0e614c54961e35fdc19fa2478d98`。Jobs: quality `92108479368`、
  test-footprint `92108479394`、MSRV `92108479373`、Windows MSVC `92108479405`、Linux GNU
  `92108479413`、Linux musl `92108479435`、interop `92108479334` and qualification
  `92109254228`。Markers bind SHA/run/attempt and report TCP/UDP each `12/12` plus cleanup、
  platforms `3/3` and aggregate `PASS`。
- Schema 3 integrity/change passed。The reviewed numeric result remains `REVIEW_REQUIRED` at ratio
  `2.025588`、case/support/fixture growth `4993/121/0`、five file `WARN` and one client `run.rs`
  `REVIEW_REQUIRED`。Final Architect returned `PASS_WITH_NOTES` and QA `PASS` with no blocker、major
  or minor finding。

## Boundaries

- Provider-created attempt 2 appeared after immutable attempt 1 completed。The primary did not request
  or authorize that rerun；attempt 2 is uncredited and does not replace attempt 1。Always cite the
  attempt-specific URL above rather than the generic run page。
- Performance job `92108480457` was skipped on the push event as designed。M11 requires a separate
  exact-SHA manual performance run，so neither T05 nor M11 may close yet。
- No second push、primary-requested rerun、dispatch、PR、tag、package、release or publication is
  authorized。The local documentation-only descendant must not be pushed without new explicit approval。

## Resume

After separate explicit authorization，dispatch `.github/workflows/m0.yml` at remote ref
`codex/integration/m11`，verify `GITHUB_SHA` is exactly
`6d975c1e45eb0e614c54961e35fdc19fa2478d98`，and credit only one immutable run/attempt whose
`performance` job and exact completion marker pass。Do not push or rerun。Then perform final
exact-evidence review and close T05/M11 locally if no blocking finding remains。
