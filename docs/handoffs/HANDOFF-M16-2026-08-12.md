# Handoff — M16 closed

- Exact product: `98800a77877de7e5b16491df9a65c635393c91f0`
- Product tree / parent: `92c6da7c7b23fd5ce348881325e3cc4b5c7b9bb0` /
  `a2ba4da191b642fef852d104df335dcea1224eaa`
- Automatic evidence: [run `31567846180/1`](https://github.com/zzffu/ferrum2/actions/runs/31567846180)
- Full hosted evidence: [run `31567877969/1`](https://github.com/zzffu/ferrum2/actions/runs/31567877969)
- Performance evidence: [run `31567880517/1`](https://github.com/zzffu/ferrum2/actions/runs/31567880517)
- State: M16-T01～T07 done；M16 closed

## Completed and verified

The qualified schema-v2 client provides route-selectable Direct/Shadowsocks egress，compatible managed IPv4
Wintun capture、physical first-hop binding and owned IPv4 DNS steering。Windows TUN-selected Direct IPv6
fails before socket creation；M15 manual-route and non-managed IPv6 behavior remain。Strict anti-leak、managed
IPv6、per-target multihomed dynamic binding and performance improvement are not claimed。

Fresh exact-candidate current-VM full and hard-kill profiles passed `16/16` retained M15 transport，Direct
TCP/UDP `1/1`，DNS `2/2`，network change `3/3`，100 cycles，three hard-kill cases and cleanup with zero
pre-remediation residue and pristine final restore。Serial local Full、Rust 1.97.1、ignored lifecycle、docs and
architecture gates passed。Automatic、full and performance hosted runs above all completed SUCCESS；the later
provider-created master run `31571017229/1` also passed and is supplemental only。

Final Architect and QA both returned `PASS` with `0/0/0/0` findings。Footprint integrity/category/ratio passed
at code/tests `30395/56993`，ratio `1.875078` and case/support/fixture delta `+6673/-3/0`。The numeric advisory
is accepted for required evidence in existing seams；no fixture、dependency or second harness was added。

## Boundary

The exact product remains on `origin/codex/integration/m16` and is the qualified ancestor of `origin/master`；
the documentation-only closeout on `master` does not replace that candidate or require VM/hosted evidence to
be relabelled。Earlier failed VM/controller
attempts and hosted runs `31549650201/1` and `31561724060/1` remain uncredited。All authorized ordinary pushes
and dispatches are consumed；no rerun、force-push、PR、tag、package、release、publication or Wintun
redistribution occurred。Deferred scope remains in the milestone and ADRs；there is no M16 review or workflow
debt。
