# Handoff — M14 closed

- Exact product: `bc6963472d9ae8e3c84d82851fd64d78c9f2a65f`
- Product tree: `a5533723d251b62529daa767dd083404fa0a30bc`
- Automatic evidence:
  [run `31284062682/1`](https://github.com/zzffu/ferrum2/actions/runs/31284062682)
- Manual performance/resource evidence:
  [run `31284310711/1`, job `93170454318`](https://github.com/zzffu/ferrum2/actions/runs/31284310711)
- State: M14-T01～T09 done；M14 closed

## Completed

- One core ordered program now owns bounded ordinary policy continuation；DNS policy remains separate。
- A pure bounded Hickory/rustls/httparse module provides DNS、TLS ClientHello and HTTP/1 metadata without
  moving concrete protocols into core。
- Server routing/sniffing runs only after SIP022 authentication and preserves exact prefix replay、UDP
  prepare/reserve/commit and terminal no-fallback semantics。
- Client TCP/UDP route、reject and DNS hijack share existing egress/DNS seams。Schema-v2 UDP selects one
  terminal action/outbound per association；schema-v1 routed+enabled UDP fails before side effects。
- Closed telemetry、owner limits、redaction、real-process、architecture and qualification mutations cover
  the new paths without a second route engine、DNS implementation、SIP022 path、fixture or harness。

## Verified

- All focused and serial local gates passed on the exact product，including workspace all-features
  `413 passed / 5 ignored`、Rust 1.88、100+ lifecycle `1/1`、docs and schema-3 integrity。Final Architect
  and QA both returned `PASS_WITH_NOTES` with zero finding。
- Automatic `31284062682/1` passed quality、footprint、MSRV、Windows/GNU/musl、SIP022 TCP/UDP each
  `12/12` plus cleanup、all 12 CoreDNS/BIND DNS cases and aggregate qualification。
- Manual `31284310711/1` passed all nine jobs。Performance recorded medians
  `136915626/479269137` B/s、ratio `0.285675867` over 10 trials；resource recorded 10,000 sessions、180
  samples、RSS `6/6` and drain；DNS recorded `4593/4481` direct/detoured queries、48 samples、RSS
  `12/12`、bounds/drain/rebind。THP restore and cleanup passed。The ratio is diagnostic only。
- Final code/tests are `25586/45009`，ratio `1.759126` PASS。The cumulative case/support/fixture delta
  `+5377/0/0` advisory is accepted required evidence；integrity passes。

## Preserved failure and boundary

Automatic run `31282591585/1` remains failed on `9a7797f…` and was never rerun。Its hosted generators
used schema v1 for routed+enabled UDP；the reviewed three-file test/tool repair moved only those generated
clients to schema v2 and retained neighboring v1 cases。Two necessary non-force pushes and one manual
dispatch were consumed。No force-push、PR、tag、package、release or publication occurred。

This closeout is documentation-only and does not replace the qualified product SHA。No M14 action or
debt remains；deferred groups、fallback/retry、TUN/transparent inbounds and management APIs remain explicit
non-goals。
