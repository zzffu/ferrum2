# Handoff — M7

- Accepted commit: `b3b99a15aa99f8393f99f4c72c85f451a48c6749`
- Tree: `018a6ed1d6547771cd9921e51d2bc8668305b6b2`
- State: closed
- Remote actions: two authorized non-force pushes completed；both scopes consumed

## Completed

- [M7](../milestones/M7-tagged-static-composition.md) added bounded tagged static
  multi-inbound/outbound composition while preserving legacy schema v1、one process-wide
  method/PSK、aggregate owners and the existing `ProcessSupervisor` transaction。
- [M7-T05](../tickets/M7-T05-replace-test-budget-with-milestone-envelope.md) replaced the
  cross-milestone permanent-ratio defect with the schema 2 exact-base Budget envelope。
- [M7-T08](../tickets/M7-T08-make-udp-owner-reap-test-deterministic.md) removed the Linux UDP
  owner-reap scheduling assumption without changing product behavior。

## Verified

- Exact `b3b99a15` passed serial local format、Clippy、workspace binaries、Full、100+ lifecycle、
  docs、Rust 1.85、focused Windows/Linux、Linux stress `2000/2000` and Budget。
- Automatic run [`30812399038/1`](https://github.com/zzffu/ferrum2/actions/runs/30812399038)
  passed quality、MSRV、Windows/GNU/musl、TCP/UDP each `12/12` plus cleanup and Budget。
- Blocking findings are zero；Budget passed at growth `863/864` with remaining `1`。

## Remaining

- Performance was excluded by the close contract and is not credited or claimed。
- Dynamic routing、DNS、multi-upstream groups/chaining、per-entry PSK/method、new endpoint kinds、
  transparent/TUN、hot reload、management API、package、release and publication remain out of scope。

## Resume

Start a separately contracted milestone and obtain fresh authorization for every remote action；
do not reuse either consumed M7 push scope。
