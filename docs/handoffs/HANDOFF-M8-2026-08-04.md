# Handoff — M8

- Accepted commit: `926843d61fcfac094765b5d1032b7239e3d9370c`
- Tree: `6511246f4e3d9be8f3e6e0730d2f6b2da41cb3bf`
- State: closed
- Remote actions: two authorized non-force pushes completed；both scopes consumed

## Completed

- [M8](../milestones/M8-first-match-routing.md) added one shared bounded first-match route table for
  client/server TCP flows and validated UDP datagrams while preserving legacy/M7 static behavior、
  SIP022 wire/security、aggregate owners and process lifecycle。
- [M8-T04](../tickets/M8-T04-qualify-first-match-routing.md) proved routed real-process、native and
  pinned external behavior；a final test-only repair removed localhost resolver-order dependence
  without changing route semantics。

## Verified

- Exact `926843d61` passed local format、Clippy、workspace binaries、Full、100+ lifecycle、docs、
  Rust 1.85、focused qualification and Budget `837/840` with remaining `3`。
- Automatic run [`30848182146/1`](https://github.com/zzffu/ferrum2/actions/runs/30848182146)
  passed quality、MSRV、Windows/GNU/musl、TCP/UDP each `12/12` plus cleanup、Budget、performance
  regression and final qualification。Architect returned `PASS_WITH_NOTES` and QA returned `PASS`；
  blocking findings are zero。

## Remaining

- M8 adds no performance threshold or claim；the successful performance job is regression evidence。
- Geo/DNS/user/domain-pattern routing、multi-upstream groups/load balancing/chaining、per-entry
  credentials、new endpoint kinds、transparent/TUN、hot reload、management API、package、release
  and publication remain out of scope。

## Resume

Start a separately contracted milestone and obtain fresh authorization for every remote action；
do not reuse either consumed M8 push scope。
