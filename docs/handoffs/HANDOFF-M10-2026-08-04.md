# Handoff — M10

- Qualified exact: `2df141e720580d2f8fe16bee6c9698f4e85a520a`
- Product ancestor: `93ed9d91929200a1786694ffd59e491b7188a5d1`
- Hosted evidence: [run `30906020944/1`](https://github.com/zzffu/ferrum2/actions/runs/30906020944)
- State: M10 closed；M10-T01/T02/T03 done
- Remote boundary: the authorized one non-force push is consumed

## Completed

- T01 added the bounded selector graph、additive config and public atomic control；T02 proved existing
  client/server TCP/UDP selection-call snapshot behavior without production changes or a new harness。
- T01 Architect `PASS_WITH_NOTES` / final QA `PASS` closed `QA-M10T01-001`；T02 Architect and QA
  both `PASS_WITH_NOTES` with no blocker、major or minor finding。
- T03 qualified the exact integration SHA with all nine current workflow results and final Architect
  `PASS_WITH_NOTES` / QA `PASS`；blocking findings are zero。

## Verified

- Public selector `2/2`、four data-plane rows `1/1` each、real-process TCP/UDP `1/1` each、
  architecture `9/9` and Rust 1.85 passed。
- Serial Full passed format、Clippy、binaries、workspace `308 passed / 5 ignored`、lifecycle `1/1`
  in `126.57s`、docs and diff checks。
- Exact-product Architect inspection of target `93ed9d9`、tree `ded9f1e`、parent `c55c6c8`
  returned `PASS_WITH_NOTES` with no new blocker、major or minor；it is local architecture evidence
  only，not hosted qualification or closure。
- Reviewed T03 repair `a274a7cdb71ad74af5b2b8bb36cd2f32d2b96396` received targeted Architect
  and QA `PASS`；`ARCH-M10T03-001` and `M10-T03-QA-001/002/003` are resolved。The integration
  branch was fast-forwarded to that repair，then diff-check and both footprint gates passed。
- Automatic push run `30906020944/1` bound all evidence to `2df141e...`：quality
  `91981153772`、test-footprint `91981153797`、MSRV `91981153779`、Windows MSVC
  `91981153760`、Linux GNU `91981153841`、Linux musl `91981153800`、interop
  `91981153770`、performance `91981153828` and qualification `91991168302` all succeeded。
- Exact markers report Full/security/process、schema 3 footprint、three platforms、TCP/UDP each
  `12/12` plus cleanup、10,000 sessions/180 samples/drain and final aggregate `PASS`。Workspace
  `308 passed / 5 ignored` and lifecycle `1/1` in `127.38s` passed locally on the exact SHA。
- Footprint is code/tests `16646/27319`、ratio `1.641175`、cumulative case/support/fixture
  `403/0/0`；T03 adds product/test `0/0` and case/support/fixture `0/0/0`。Integrity/change are
  `PASS`；`ARCH-M10T02-001` and
  `M10-T02-QA-N01` accept the two large-file `REVIEW_REQUIRED` signals。

## Boundaries

- Performance completed in `42m51s` with ferrum/reference medians `147499690/477779285` B/s and
  ratio `0.308719307`。It is regression/aggregate evidence only；M10 adds no threshold or claim。
- Automatic selection、retry、health/load balancing、external control、persistence and connection
  interruption remain out of scope。No second push、rerun、dispatch、PR、tag、release or publication
  is authorized by this closeout。

## Resume

No M10 continuation is required。Remote `codex/integration/m10` preserves the qualified exact SHA；the
dedicated closeout is a local documentation-only descendant and must not be pushed without new explicit
authorization。A future request for automatic upstream policy or external selector control requires a
new contract rather than reopening M10 implicitly。
