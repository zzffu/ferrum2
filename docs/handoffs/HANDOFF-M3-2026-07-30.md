# Handoff: milestone M3

- **Date:** 2026-07-30（Asia/Shanghai）
- **Local integrated/evidence closeout source:**
  `master@d784b06171723bb93fd467cea1a799f58f7d60b0`
- **Remote-qualified product commit:**
  `d9e59d787c3fe78dfca778ee8a36668a45387368`
- **Hosted evidence:** GitHub Actions run `30494736004`, attempt `1`
- **Milestone status:** closed

## Current state

M3 有七票 `done`、一票 `deferred`：

| Ticket | Delivered outcome | Reviewed/final candidate | Durable state |
|---|---|---|---|
| M3-T01 | preserved schema-v1 cohort、stable CLI/config contract、non-exhaustive topology guards | `001047e895726debef04b31d781aa1eee73c24a9` | done |
| M3-T02 | closed tracing identity、exact fourteen metric families、redaction/cardinality sentinels | `84e5e226eb317e993991226efd104a0bbd0bf703` | done |
| M3-T03 | reusable transactional process supervisor、rollback/ownership/cancel/force/reap | `1d7111d2d20df805740c21e98da8fb161141b161` | done |
| M3-T04 | first binary-composition attempt；bounded review lifecycle exhausted | `a90c49644323c2266787c0f259aa4f482bdee60b`，not integrated | deferred |
| M3-T06 | fresh replacement delivering transactional binary composition、terminal UDP reap、fixed forced-root watchdog and production lifecycle evidence | `ed615cbcd373d882eaa236ee4556d20eb4e16e48` | done |
| M3-T05 | three-target native artifact/lifecycle/linkage qualification | `bba40d127dee29a719d6ea1d80fb10427149d890` | done |
| M3-T07 | portable UDP process evidence isolation under parallel execution | `bc14971c51982b6ad9a970593fb3848b2763b112` | done |
| M3-T08 | causal terminal-UDP readiness evidence | `d9e59d787c3fe78dfca778ee8a36668a45387368` | done |

Runtime phases and ownership leases are empty；open canonical roots are empty。
`workflow.py next --milestone M3 --json` retains the pre-close terminal action
`ready_to_close` because the helper has no separate milestone-close state
mutation。Roadmap、verified context audit and this handoff are the durable
`closed` state。

T04 remains honestly deferred rather than rewritten as PASS。T06 imported the
unintegrated T04 product patches with matching stable patch IDs，fixed the
terminal-UDP circular wait and unbounded forced-root cleanup，then completed a
fresh full/repair/targeted review lifecycle。No approved M3 product outcome is
left behind by the T04 deferral。

## Qualified product and hosted evidence

`d9e59d787c3fe78dfca778ee8a36668a45387368` is the exact product/control SHA
qualified remotely。The later local commit
`d784b06171723bb93fd467cea1a799f58f7d60b0` changes only five execution evidence
documents；it has no product、test or CI source delta and is not claimed as a
separately hosted-qualified SHA。

One authorized non-force fast-forward push of exact `d9e59d78...` to
`refs/heads/codex/integration/m3` triggered GitHub Actions
[run 30494736004 attempt 1](https://github.com/zzffu/ferrum2/actions/runs/30494736004)。
The push event and all seven required jobs completed `success` on that exact
SHA/run/attempt：

| Result | Job ID |
|---|---:|
| `quality` | `90720794923` |
| `msrv` | `90720794873` |
| `platform / windows-msvc` | `90720794992` |
| `platform / linux-gnu` | `90720795107` |
| `platform / linux-musl` | `90720794921` |
| `interop` | `90720794966` |
| `qualification` | `90721365575` |

The run proves native execution of both release binaries、offline config、
startup rollback、genuine graceful/forced signal handling、bounded cleanup and
immediate exact rebind；two artifacts per target have SHA-256 evidence，with
PE、ELF/GLIBC and static/static-PIE musl linkage assertions。TCP and UDP interop
are each `12/12` with cleanup PASS。Missing/skipped required profiles do not
count as PASS；none was missing。

Failed runs `30472227257/1` at `bba40d12...` and `30476271774/1` at
`bc14971c...` remain immutable failed evidence。No passing rows were spliced
from them into the accepted run。

## Exit criteria

| Exit criterion | Primary evidence | Result |
|---|---|---|
| Parser-accepted schema-v1 cohort and evolution direction | ADR-0023；T01 cohort/default/boundary/invalid/redaction table | PASS |
| Current topology remains non-exhaustive | T01 architecture/dependency/deep-module guards | PASS |
| Stable CLI/exits/codes/redaction and validation before resources | T01/T06 config/CLI/process tables and occupied-resource zero-side-effect rows | PASS |
| Closed traces and fourteen metric identities | T02 exact identity、encoding、1,000-destination and secret sentinels | PASS |
| Reusable transactional supervisor | T03/T06 state、rollback、root arbitration、owner、deadline、forced cleanup evidence | PASS |
| Production adapters and at least 100 bounded cycles per binary | T06 process-cycle、TCP half-close、UDP enabled/disabled、fatal-root and rebind evidence | PASS |
| Windows MSVC、Linux GNU、Linux musl native artifacts | T05 plus run `30494736004/1` native lifecycle/hash/linkage rows | PASS |
| Exact-SHA/run/attempt convergence | exact `d9e59d78...`，seven successful jobs，TCP/UDP `12/12`，local full and milestone budget | PASS |

SPEC-0004 M3-MUST-01～10 and TEST-0004 evidence mappings are satisfied。M3 did
not implement or claim routing、DNS、public UDP inbound、TUN、transparent
inbound、SIP023/multi-user、performance、packaging or publication。

## Decisions and contracts

- `ADR-0023` preserves the M3-close parser-accepted v1 cohort throughout v0.x；
  after a stable successor，removal additionally waits at least 12 months and
  two stable minor releases and requires prior stable-release notice。
  Compatible v1 evolution is optional/additive/safe widening with unchanged
  omitted-field behavior；breaking behavior requires an explicit new version，
  with no heuristic fallback、silent reinterpretation or automatic rewrite。
- `ADR-0024` establishes prepare-before-poll、reverse deterministic rollback、
  one transitive owner/cancellation lineage、one monotonic absolute grace
  deadline、grace/force/reap and explicit cleanup-failure outcomes。
- `ProcessRoot`、`PreparedProcessRoot` and `ProcessSupervisor` are the reusable
  topology-neutral lifecycle seam；the two binary `run.rs` files are current
  adapters for TCP、optional server UDP and optional metrics roots。
- Complete semantic config validation precedes subscriber/runtime construction
  and every listener、socket、metrics endpoint、session、channel or task。
  Offline validation remains side-effect free。
- CLI exits and redacted codes、closed trace fields and fourteen metric-family
  identities are stable compatibility contracts；secrets and destinations do
  not enter accepted output or metric labels。
- Current single listen/server、IPv4-validated operator endpoints、two binaries
  and workspace member set are not permanent topology。
- No ADR/SPEC amendment was required during close。

## Context audit and closeout files

All seven configured `AGENTS.md` Project-specific context entries were
re-inventoried：

| Entry | Close classification/result |
|---|---|
| Product purpose | current M3 operator-contract and native-qualification fact added |
| Primary languages/frameworks | confirmed unchanged |
| Architecture entry points | reusable process lifecycle seam and current binary adapters added |
| Critical invariants | cohort/evolution、pre-resource validation and lifecycle invariants added |
| Generated files | current no-generated-source fact and uncommitted qualification artifacts clarified |
| Local development setup | existing hosted native/interop harnesses recorded |
| Active planned changes | completed M3 removed；set to `None` |

- planning baseline context hash：
  `b58311d6db1eaffb4188ff5885aaf92169d9a8e62a79acba544b37f814483ea1`；
- approved-plan context hash：
  `aa9bd4cb87d19ca5dd134e9eb6435f45e51ae422a691903a4107bebcc9bdac86`；
- verified-close context hash：
  `ce9f3efc9040bf81dcbe8fabf6f1ece1a4dddd34667841d5cd4d292ea65ecd04`。

Closeout changes are limited to：

- `AGENTS.md` Project-specific context；
- M3 context audit、vision、gap analysis、roadmap、CI status and this handoff；
- `docs/review-debt.md`；
- mechanical T06 evidence SHA correction；
- `codex/test-budget-baseline.json`。

No product、test、CI workflow、dependency、control-plane or runtime-ledger file is
changed by close mode。

## Validation evidence

Team Lead and close reviewers recorded：

| Source | Command/check | Exit/result |
|---|---|---:|
| `master@d784b061...` clean pre-close | `workflow.py doctor` | 0；doctor checks passed |
| same | `workflow.py validate` | 0；only pre-existing M0 acceptance-count and legacy review warnings |
| same | `workflow.py control-plane-check --json` | 0；clear，no drift/violation/warning |
| same | `workflow.py context-inventory --json` | 0；7/7，approved hash `aa9bd4...` |
| same | `workflow.py context-check --strict --milestone M3 --require-audit` | 0 |
| same | `workflow.py next --milestone M3 --json` | 0；`ready_to_close`，done=7、deferred=1、no active/open root |
| same | authoritative `workflow.py run-validation full` | 0；6/6 PASS |
| same | milestone budget against `7907cda...` | 0；PASS，`12956/19861/1.533` |
| run `30494736004/1` | public run/jobs/steps read-only audit | PASS；exact SHA/event/attempt，7/7 success |
| closeout working tree | `test-budget --gate report --tool builtin --write-baseline --json` | 0；`baseline_written=true` |
| closeout working tree | authoritative `workflow.py run-validation full --cwd .` | 0；6/6 PASS in 171.2 s |

Close reviewers independently inspected source/contracts/ticket histories and
reran selected read-only checks。QA independently confirmed all hosted job IDs
and milestone budget。No reviewer modified tracked、workflow or remote state。

After advancing the accepted baseline，a redundant close-working-tree invocation
of `test-budget --gate milestone --base 7907cda...` exited `1` because the helper
correctly applied another `0.05` ratchet to the newly written M3 baseline。It is
not credited as the M3 gate and does not indicate changed counts。The
authoritative ordering is milestone gate against the retained M2 baseline，
followed by baseline write；the still-clean integration worktree at
`d784b061...` retained that M2 baseline and reconfirmed the command at exit `0`
with required ratio `1.591967`。

## Test-budget ratchet

- previous accepted baseline：M2 product
  `7907cda05a56e1c3b85af2dd8faeb85a385154b7`；
- previous code/tests/files/ratio：`11714/19234/66/1.641967`；
- M3 accepted source：
  `d784b06171723bb93fd467cea1a799f58f7d60b0`；
- M3 code/tests/files/ratio：`12956/19861/66/1.532958`；
- milestone required ratio：`1.591967`；
- planning-base delta：code `+1242`、tests `+627`；
- delta allowance：`1362`；
- milestone verdict：`PASS`；
- report baseline write：exit `0`，`baseline_written=true`。

Ratio `1.533` remains above advisory warning `0.850`，but the ratchet improved
and passed。Do not add meaningless production code to improve the ratio。

## Existing branches and worktrees

Closeout does not delete any branch or worktree：

| Worktree | Branch/HEAD | Close preflight state |
|---|---|---|
| repository root | `master@d784b061...` | clean before closeout edits |
| `.worktrees/_integration-m3` | `codex/integration/m3@d784b061...` | clean |
| `.worktrees/m3-t01` | `codex/ticket/m3-t01@001047e...` | clean |
| `.worktrees/m3-t02` | `codex/ticket/m3-t02@84e5e22...` | clean |
| `.worktrees/m3-t03` | `codex/ticket/m3-t03@1d7111d...` | clean |
| `.worktrees/m3-t04` | `codex/ticket/m3-t04@a90c496...` | clean |
| `.worktrees/m3-t05` | `codex/ticket/m3-t05@bba40d1...` | clean |
| `.worktrees/m3-t06` | `codex/ticket/m3-t06@ed615cb...` | clean |
| `.worktrees/m3-t07` | `codex/ticket/m3-t07@bc14971...` | clean |
| `.worktrees/m3-t08` | `codex/ticket/m3-t08@d9e59d7...` | clean |

Generated Cargo output、qualification logs/artifacts、packet captures and
benchmark outputs remain uncommitted。Do not reset/clean retained worktrees；
branch/worktree deletion requires separate authorization。

## Review rounds and finding history

| Scope | Authoritative bounded review history |
|---|---|
| M3-T01 | Architect/QA full `PASS` at `001047e...` |
| M3-T02 | QA full `PASS`；Architect full `BLOCK` on `ARC-M3-T02-001` at `4a9ac5d...`；one repair；Architect targeted `PASS` at `84e5e22...` |
| M3-T03 | Architect/QA full `BLOCK` at `c1cf718...` on `ARC-M3-T03-001` and derivatives `QA-M3-T03-001/002`；one combined repair；both targeted `PASS` at `1d7111d...` |
| M3-T04 | Architect/QA full `BLOCK` at `b35e809...` on `ARC-M3-T04-001/002` and `QA-M3-T04-001`；one repair；targeted `ESCALATE` at `a90c496...` under canonical `ARC-M3-T04-004`；ticket deferred |
| M3-T06 | Architect/QA full `BLOCK` at `24561cf...` on `ARC-M3-T06-001` / `QA-M3-T06-001`；one repair；both targeted `PASS` at `ed615cb...` |
| M3-T05 | Architect full `PASS`；QA full `BLOCK` at `441b290...` on `QA-M3-T05-001`；one repair；QA targeted `PASS` at `bba40d1...` |
| M3-T07 | fresh Architect/QA full `PASS` at `bc14971...` |
| M3-T08 | fresh Architect/QA full `PASS` at `d9e59d7...` |
| M3 close | Product Manager `PASS_WITH_NOTES`；Architect `PASS_WITH_NOTES`；QA `PASS`；no blocker/major |

Configured reviewer profiles were Product Manager
`product_manager/gpt-5.6-sol/medium`、Architect
`architect/gpt-5.6-sol/high` and QA `qa/gpt-5.6-sol/medium`。Actual serving
model/reasoning telemetry was not exposed and remains unverified。

Architect close note `ARCH-M3-CLOSE-N01` found one nonexistent SHA in T06
completion evidence。Closeout corrected
`263ceddf50458c49b5d73d88a33eed96e858bf3f` to the real imported commit
`263cedda699f7f2e2ef1516438c5025296e862ca`。This is mechanical and does not
change lineage、patch identity、product behavior or review state。

## Canonical roots and preserved non-PASS evidence

Current open roots：none。

- `ARC-M3-T02-001` and `ARC-M3-T03-001` plus QA derivatives were resolved by
  their sole repairs and targeted PASS reviews。
- T04 review roots terminate in the recorded `ESCALATE`/deferral。They are not
  rewritten；T06 is the separately authorized replacement with fresh reviews。
- `ARC-M3-T06-001` / `QA-M3-T06-001` and `QA-M3-T05-001` were resolved by
  their sole repairs and targeted PASS reviews。
- `HOSTED-M3-T05-001` and `HOSTED-M3-T07-002` were resolved by later exact-SHA
  evidence；the two earlier hosted runs remain failed。
- T07/T08 have no review finding IDs。

No failed、superseded、deferred or skipped evidence was deleted or converted to
PASS。

## Authorization scopes and remote state

All eight M3 authorization scopes are exhausted and revoked：

| Scope | Kind | Use/final state |
|---|---|---|
| `AUTH-M3-T04-001` | local | 1/1；revoked |
| `AUTH-M3-T06-001` | local | 1/1；revoked |
| `AUTH-M3-T05-LOCAL-001` | local | 1/1；revoked |
| `AUTH-M3-T05-REMOTE-001` | remote exact `bba40d12...` | 1/1；revoked |
| `AUTH-M3-T07-LOCAL-001` | local | 1/1；revoked |
| `AUTH-M3-T07-REMOTE-001` | remote exact `bc14971c...` | 1/1；revoked |
| `AUTH-M3-T08-LOCAL-001` | local | 1/1；revoked |
| `AUTH-M3-T08-REMOTE-001` | remote exact `d9e59d78...` | 1/1；revoked |

The remote scopes authorized only one non-force fast-forward each to
`refs/heads/codex/integration/m3` plus read-only monitoring。They do not
authorize another push、rerun、dispatch、remote `master`、PR、tag、release、
publication、upload、signing、force-push or deletion。

Close mode performed no remote mutation。The docs-only closeout commit is not
pushed or published。

## Known risks and debt

- ADR-0023's 12-month/two-minor/prior-notice window remains a future release
  obligation；M3 proves the policy and preserved-cohort guards，not elapsed time。
- M4 must freeze reproducible hardware、reference version/config、load、
  warm-up/repetition/statistics and resource thresholds before performance
  claims。
- Test/code ratio remains above the advisory warning threshold，although the
  ratchet passed。
- The helper continues to report pre-close `ready_to_close` for closed
  milestones；durable close truth is roadmap/context/handoff。
- No unresolved M3 P0/P1 product、security、lifecycle、platform or release
  blocker remains。

## Deferred work

- M4：comparable loopback TCP throughput at least 90% of the frozen
  shadowsocks-rust baseline；10,000 idle TCP task/RSS stability；long soak；
  same-commit integrated v0 qualification。
- Future feature planning：multi-inbound/outbound、routing、DNS、multiple
  upstreams/load balancing/chaining、Linux transparent inbound、Windows TUN、
  public UDP inbound、SIP023/multi-user、hot reload and management API。
- Release operations：archives、installers、signing、upload、tag、release and
  publication。None occurred in M3 close。

## Recovery instructions

Audit M3 closeout：

```powershell
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py doctor
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py validate
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  control-plane-check --json
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  context-inventory --json
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  context-check --strict --milestone M3 --require-audit
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  status --milestone M3
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  state --milestone M3
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  next --milestone M3 --json
python -X utf8 .agents/skills/milestone-workflow/scripts/workflow.py `
  test-budget --gate report --tool builtin --json
```

Do not reset/clean retained worktrees，reuse exhausted authorization，splice
different SHA/run/attempt evidence，rewrite T04 as PASS，or push the docs-only
closeout commit without new explicit authority。

## Next recommended action

The next entry is：

```text
$milestone-workflow
mode: plan
milestone: M4
goal: Freeze reproducible throughput, task, memory, 10,000-idle-session and
long-soak qualification contracts on top of the closed M3 operator and lifecycle
surface, without publishing artifacts or adding deferred topology features.
```

This entry starts M4 contract planning only；it does not automatically
implement、push、open a PR、tag、release or publish。
