# Handoff — M13 closed

- Exact product: `1af1bbf44b37a81c2ae03c562288b2a6e09694b5`
- Product tree: `172870c4ca0dffb6c474f2137399d553e827b1e4`
- Remote branch: `codex/integration/m13` points exactly to the product
- Automatic evidence:
  [run `31223817144/1`](https://github.com/zzffu/ferrum2/actions/runs/31223817144)
- Manual performance/resource evidence:
  [run `31223831024/1`, job `93013935727`](https://github.com/zzffu/ferrum2/actions/runs/31223831024)
- State: M13-T01～T07 done；M13 closed

## Completed

- Core now owns the only allocation-preserving `EgressPlanSnapshot` while borrowed plan APIs remain
  compatible。
- DNS owns its runtime model and depends internally on core rather than config DTOs；both binaries perform
  pure conversion before side effects。
- SOCKS and DNS share one private concrete client TCP executor and one bounded UDP association；caller
  policy、ordered credentials、snapshot identity and exact DNS reuse keys remain explicit。
- Client/server/core/config implementation and tests live with their owners；binary roots retain process
  composition only。No new product behavior、crate、third-party dependency、unsafe、fixture、harness、
  trait、factory or registry was added。

## Verified

- Every T01～T06 focused command and the exact T07 serial local gate passed，including workspace
  `372 passed / 5 ignored`、Rust 1.88 check/build/test、lifecycle `1/1` and docs。Final full Architect and
  QA both returned `PASS` with zero blocking finding。
- Automatic `31223817144/1` passed quality、schema 3、MSRV、Windows/GNU/musl、TCP/UDP `12/12` each plus
  cleanup、all 12 CoreDNS/BIND DNS cases plus cleanup and aggregate qualification。
- Manual `31223831024/1` passed all nine jobs。Performance reports medians `144633582/546819276`、ratio
  `0.264499787` and 10 trials；legacy resources report 10,000 sessions、180 samples、RSS `6/6` and drain；
  DNS reports direct/detoured `4600/4466` queries、48 samples、RSS `12/12`、bounds/drain/rebind。THP and
  cleanup restoration passed。
- Final schema 3 code/tests are `21814/39632`，ratio `1.816815` PASS，case/support/fixture delta
  `-116/0/0`。The moved-file numeric review is accepted ownership reclassification with `92/92` product
  test names and unchanged assertions。

## Repair and boundary

T05 repaired a copied plan allocation and stale healthy-pool state。T06 repaired shallow module/test
ownership，then the required two independent read-only analyses identified and closed the remaining
architecture-scanner false negatives。All failed/review evidence remains recorded in the ticket、roadmap
and CI status。

The authorized one product push and one manual dispatch are consumed；no unchanged-SHA rerun、second
push/dispatch、PR、tag、package、release or publication occurred。The closeout commit containing this file
is documentation-only and does not replace the qualified product SHA。`CONTEXT.md` already defines the
owned immutable egress-plan contract used by this product，so close audit required no glossary mutation。
No M13 action remains。
