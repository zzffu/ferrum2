# Handoff: milestone M1

- **Date:** 2026-07-28（Asia/Shanghai）
- **Base branch/closeout source:**
  `master@dd17233e292262c80bfd8f0e5a0db4bc0361244e`
- **Product/release commit:**
  `874c83d0ee71054bd702d6ecac55e88d9e2fbcef`
- **Milestone status:** closed

## Current state

M1 的四个 in-scope tickets（M1-T01～M1-T04）均为 `done`，四项 release
dependency checks 全部 clear，runtime phases 为 `{}`，没有 open canonical root。
产品与 release 证据绑定到 exact
`874c83d0ee71054bd702d6ecac55e88d9e2fbcef`；closeout source
`dd17233e292262c80bfd8f0e5a0db4bc0361244e` 是只修改 roadmap、CI status 和
T04 completion evidence 的 docs-only 后代，不是新的 product/release SHA。

GitHub Actions
[run 30367147537 attempt 1](https://github.com/zzffu/ferrum2/actions/runs/30367147537)
由 exact `874c83d` 的一次授权 push 触发。public REST 与登录态 job log 的只读
复核确认 run 为 `push` event、`completed/success`，且同一 run/attempt/SHA 的
六项 required results 全部 `completed/success`：

| Result | Job ID |
|---|---:|
| `quality` | `90301213202` |
| `msrv` | `90301213253` |
| `platform / windows-msvc` | `90301213226` |
| `platform / linux-gnu` | `90301213397` |
| `platform / linux-musl` | `90301213284` |
| `interop` | `90301213129` |

`interop` 的 exact-SHA clean guard、两个 pinned references provisioning 与 hosted
qualification steps 均 success；authenticated raw log 中 M1-INT-001～012 各有
一条 `status=PASS`，没有 FAIL、missing 或 duplicate。`quality` raw log 中
`success_bounded_method_matrix_preserves_bytes_and_half_close` 恰出现一次，
`SKIP real-process IPv6 row: host IPv6 loopback connect unavailable` 出现零次；
因此 Linux hosted run 实际执行了本机 Windows 无法执行的 real-process IPv6 row。

## Completed work

| Ticket | Delivered outcome | Candidate | Durable state |
|---|---|---|---|
| M1-T01 | method-bound AES-128/AES-256/ChaCha crypto profiles、reviewed fixtures、dependency/zeroize policy | `4223051eeae35220b150461cad91daf09a954423` | done |
| M1-T02 | one shared SIP022 TCP flow、IPv4/IPv6/domain target path、bounded resolution/deadline/reply | `ae84631c515933f60b2aa3f898a86fa3cff11ce9` | done |
| M1-T03 | method-aware config/binary composition、local product/lifecycle matrix | `4c9ad421e0ef5d193e29e70ed5a674cb30a4aa88` | done |
| M1-T04 | fail-closed 12-cell hosted TCP qualification and thin CI profile | `b7a69899e4053e78fe8824e2cd9215b9d232e106` | done |

M1 roadmap exit criteria：

| Exit criterion | Primary evidence | Result |
|---|---|---|
| Three methods：KDF/AEAD、SIP022 positive/negative、replay、binding、detection | T01/T02 parameterized crypto and shared-flow suites；exact-SHA local/hosted full | PASS |
| IPv4/IPv6/domain、invalid address、refusal、half-close、cancellation/error mapping | T02 address/order/deadline tables；T03 real-process/lifecycle matrix；hosted IPv6 marker audit | PASS |
| `3 × 2 × 2 = 12` TCP interop cases with no silent skip | fixed M1-INT-001～012 plan、pure fail-closed aggregation、exact hosted raw log | PASS |
| M0 security/resource/observability/platform/full regression | exact-SHA close full 4/4、Rust 1.85、Windows/GNU/musl、quality job | PASS |

SPEC-0002 M1-AC-01～08 均有 TEST-0002 指定的 primary evidence；Product Manager、
Architect 和 QA close review 都确认 AC matrix PASS，没有新 blocker 或 major
finding。

## Decisions and contracts

- `ADR-0018`：method-bound secret/profile，16-byte AES-128 与 32-byte
  AES-256/ChaCha，crypto owner 内部 enum dispatch，一个 shared TCP state path。
- `ADR-0019`：normalized IPv4/IPv6/1～255-byte ASCII domain，最多 16 个 ordered
  resolver candidates，一个 absolute deadline，actual `SocketAddr` reply。
- `SPEC-0002` 和 `TEST-0002` 保持 Approved；closeout 没有修改 wire、public API、
  security ordering、error semantics、dependency graph、reference pins 或 scope。
- M1 仍是 single PSK；SIP023/multi-user 没有被暗中加入。
- configured listener/SS-server endpoint 继续 IPv4-only；M1 扩展的是 application
  target。
- 不新增 ADR。三个 close reviewer 均确认现有 decision 不矛盾。

## Validation evidence

Team Lead 在 close mode 实际执行：

| Commit/worktree | Command | Exit/result |
|---|---|---:|
| `master@dd17233` | `python --version` | 0；Python 3.11.9 |
| same | `python .agents/skills/milestone-workflow/scripts/workflow.py doctor` | 0 |
| same | `python .agents/skills/milestone-workflow/scripts/workflow.py validate` | 0 |
| same | `workflow.py test-budget --gate report` | 0；`7720/15759/2.041321` |
| same | `workflow.py status`、`worktree-list`、`state --milestone M1` | 0 |
| same | `workflow.py next --milestone M1 --json` | 0；pre-close `ready_to_close` |
| same | M1-T01～M1-T04 各自 `gate-check <ticket> release --json` | 0；4/4 clear |
| `codex/integration/m1@874c83d` | `cargo build --workspace --bins --locked` | 0 |
| same | `cargo fmt --all -- --check` | 0 |
| same | `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | 0 |
| same | `cargo test --workspace --all-features --locked` | 0；92.6s |
| same | `cargo doc --workspace --all-features --no-deps --locked` | 0 |
| same | `git diff --check` | 0 |
| same | `workflow.py test-budget --gate milestone --base 8318ef106d6cd4e029bd3b02aa64125fabdda462 --json` | 0；PASS |
| run `30367147537` | public GitHub run/jobs REST read | 0；exact SHA/attempt，6/6 success |
| quality job `90301213202` | authenticated raw-log marker audit | PASS；test 1、skip 0 |

第一次 `cargo test --workspace --all-features --locked` 调用在 60 秒到达执行 wrapper
上限并返回 124，没有产生 test verdict；同一未修改 SHA 的捕获式重跑在 92.6 秒
exit 0。该项是 environment/setup derivative evidence，不是 product failure 或
repair event。

close gate 的 pre-update milestone budget：

- code `7720`；
- tests `15759`；
- ratio `2.041321`；
- previous baseline `7031/14707/2.091737`；
- required ratchet ratio `2.041737`；
- planning-base delta `+689/+1052`；
- verdict `PASS`，没有 waiver。

三个 close reviewer 接受 exact candidate 后，
`codex/test-budget-baseline.json` 以
`master@dd17233e292262c80bfd8f0e5a0db4bc0361244e` 为 accepted source 更新为
`7720/15759/2.041321`。baseline 更新命令为：

```powershell
$env:PYTHONUTF8='1'
python .agents/skills/milestone-workflow/scripts/workflow.py `
  test-budget --gate report --tool builtin --write-baseline --json
```

closeout documents/baseline candidate 的 workflow unit tests `66/66` PASS，
`workflow.py validate` exit 0，post-ratchet `test-budget --gate report` exit 0；
staged whitespace evidence 在 closeout commit 前由 Team Lead 重新执行。

## Existing branches and worktrees

closeout 没有删除 branch 或 worktree。主要 durable pointers：

| Worktree | Branch/HEAD | Tracked state before closeout commit |
|---|---|---|
| repository root | `master@dd17233` | closeout docs/baseline working tree |
| `.worktrees/_integration-m1` | `codex/integration/m1@874c83d` | clean |
| `.worktrees/m1-t01` | `codex/ticket/m1-t01@4223051` | clean |
| `.worktrees/m1-t02` | `codex/ticket/m1-t02@ae84631` | clean |
| `.worktrees/m1-t03` | `codex/ticket/m1-t03@4c9ad42` | clean |
| `.worktrees/m1-t04` | `codex/ticket/m1-t04@b7a6989` | clean |
| `.worktrees/m1-review-override-control` | `codex/workflow/m1-t02-review-override@a294e15` | clean |
| `.worktrees/m1-test-budget-control` | `codex/workflow/m1-test-budget-gate@81345fb` | clean |

integration validation 生成的 ignored `target/` 是 disposable Cargo output，没有
提交。不要用 forced cleanup；如需删除 retained worktree/branch，须另行明确授权。

## Active execution phases and ownership

- Runtime ledger phases：`{}`。
- Active ownership leases：none。
- Active Engineer/reviewer work：none。
- close mode 没有 implementation wave；close waves=`0`。
- execute history 的最后 checkpoint 是 wave `5`、action `ready_to_close`、
  no-progress count `0`。
- durable roadmap scheduler state：`closed`；helper 的 pre-close terminal action
  保留为 `ready_to_close`，因为 helper 没有单独的 milestone-close mutation。

## Review rounds and finding history

| Scope | Review evidence |
|---|---|
| M1-T01 | Architect full `PASS`；QA full `PASS_WITH_NOTES`；`QA-M1-T01-001`；无 repair/targeted |
| M1-T02 | Architect/QA full `BLOCK` at `2d5dfa2`；QA targeted `PASS`、Architect targeted `ESCALATE` at `6e8e3ed`；Architect authorized superseding `PASS` at `ae84631` |
| M1-T03 | Architect/QA full `PASS_WITH_NOTES`；`ARCH-M1-T03-001`、`QA-M1-T03-001/002`；无 repair |
| M1-T04 | Architect/QA full `PASS`；无 finding/repair |
| M1 close | Product Manager、Architect、QA 各一次 read-only close review，均 `PASS_WITH_NOTES`；无 targeted round |

T02 stable blocking IDs `ARCH-M1-T02-001` 和 `QA-M1-T02-001` 属于 canonical root
`M1-T02-REVIEW-001`。full、targeted、superseding 历史全部保留，没有被 closeout
覆盖或重新解释。closeout 没有调用额外 generic code-review system。

## Canonical blockers and derivatives

当前 open roots：none。

历史 root `M1-T02-REVIEW-001`：

- class/risk/gate：`security` / `critical` / review；
- derivatives：
  `ARCH-M1-T02-001`（peer-owned domain allocation/order evidence）和
  `QA-M1-T02-001`（reverse-dependent compilation/profile matrix）；
- first repair `6e8e3ed` 后 QA finding resolved、Architect finding escalated；
- user-authorized additional repair `ae84631` 与 Architect superseding review
  resolved 原 finding；
- final resolution：workspace/ticket/security-order gates pass；
- substantive budget `2/2` consumed，包括一个 exact root-bound override；
- 当前 status `resolved`。

`CTRL-M1-T02-001/002` 是 integration 前已关闭的 workflow-control hardening
findings。close mode 的 60 秒 wrapper timeout 是已由同-SHA successful rerun
关闭的 environment derivative，不产生 product root 或 repair attempt。

## Active authorization scopes

Active scopes：none。

| Scope | Kind/action | Final state/use |
|---|---|---|
| `m1-t02-review-001-repair-override-20260728-a1` | local / `repair_budget_override` | revoked；1/1 |
| `m1-t02-arch-001-control-amendment-ae84631-20260728-a1` | local / `workflow_control_amendment` | revoked；1/1 |
| `m1-t02-arch-001-review-round-override-ae84631-20260728-a1` | local / `review_round_override` | revoked；1/1 |
| `m1-20260728-remote-qualification-874c83d-a1` | remote / `remote_qualification_push` | revoked；1/1 |

remote scope 只绑定
`refs/heads/codex/integration/m1@874c83d0ee71054bd702d6ecac55e88d9e2fbcef`。
它不授权第二次 push、rerun、remote `master`、PR、tag、release、publish、branch
deletion 或其他 remote/destructive action。close mode 没有创建或消费 authorization。

## Agent and gate provenance

`workflow.py doctor` 确认 configured profiles：

| Gate | Requested/configured profile | Observable runtime profile | Verdict |
|---|---|---|---|
| Product closeout | `product_manager / gpt-5.6-sol / medium` | unverified | PASS_WITH_NOTES |
| Architecture closeout | `architect / gpt-5.6-sol / high` | unverified | PASS_WITH_NOTES |
| QA closeout | `qa / gpt-5.6-sol / medium` | unverified | PASS_WITH_NOTES |

三个角色由 exact named `agent_type` 启动；collaboration runtime 没有向 Team Lead 或
reviewer 暴露实际 serving model/reasoning telemetry，因此不能把 configured profile
声称为 observed actual。三个角色只读，没有 tracked edit、ledger mutation、merge、
push 或 publish。

## Known risks and debt

- `ARCH-M1-N01`：只保证 drop/终止 ferrum-owned resolver future、candidate socket
  和 flow work，不夸大为可移植地强制取消已进入 OS 的 resolver syscall。
- `QA-M1-T01-001`：clean process tests 需要先 build binaries；local close 与 hosted
  quality 都使用明确 build ordering。
- `ARCH-M1-T03-001`、`QA-M1-T03-001`：Windows 本机 real-process IPv6 row 没有
  执行；hosted Linux quality raw log 已证明 exact-SHA row 执行，历史本机限制仍保留。
- `QA-M1-T03-002`：本机 platform artifact run 因 fixed port 1080 被占而
  setup-blocked；exact-SHA hosted 三平台结果满足 M1 release evidence。
- `QA-M1-CLOSE-A01`：roadmap、CI、handoff 和 baseline 的程序性 close action 已由
  本 closeout diff 完成；不是 candidate defect。
- test/code ratio `2.041321` 仍高于 warning ratio `0.85`；ratchet gate 已通过且
  baseline 前进约 `0.050416`，不得通过无意义 production code 改善比例。
- M1 没有 unresolved P0/P1 product blocker。完整 v0 仍未完成。

## Deferred work

- M2：三个方法的 SIP022 UDP protocol API、per-direction replay window、bounded
  session/buffer/idle lifecycle、direct UDP path 与 12 项 UDP interop；不暗示公开
  UDP inbound。
- M3：最终 operator-facing config/CLI/observability contract、长期 lifecycle/
  soak 与 Windows/GNU/musl native packaging qualification。
- M4：可复现 throughput ≥ comparable shadowsocks-rust 90%、10,000 idle
  stability 与同一 commit 的 v0 integrated qualification。
- v0 non-goals 继续包括 SIP023/multi-user、SOCKS5 UDP ASSOCIATE、routing、DNS
  proxy/cache、multi-upstream/load balancing、proxy chaining、hot reload、
  management API、reduced-round ChaCha、custom executor 与 `io_uring`。

## Recovery instructions

需要审计 M1 closeout 时：

```powershell
python .agents/skills/milestone-workflow/scripts/workflow.py doctor
python .agents/skills/milestone-workflow/scripts/workflow.py validate
python .agents/skills/milestone-workflow/scripts/workflow.py status
python .agents/skills/milestone-workflow/scripts/workflow.py worktree-list
python .agents/skills/milestone-workflow/scripts/workflow.py state --milestone M1
python .agents/skills/milestone-workflow/scripts/workflow.py next --milestone M1 --json
```

不要 recreate/reset 现有 worktree，不要复用已耗尽 authorization，不要把 Windows
fallback 改写为 local IPv6 PASS，也不要把不同 SHA/run/attempt 的 job 结果拼接。

## Next recommended action

M1 closeout commit 后，下一入口是：

```text
$milestone-workflow
mode: plan
milestone: M2
goal: Deliver the three SIP022 UDP method paths through the bounded protocol API and direct server path, with per-direction replay/session limits and the complete 12-case UDP interoperability matrix, without adding a public UDP inbound.
```

该入口只开始 M2 contract planning；不自动实施、push、开 PR、tag、release 或
publish。
