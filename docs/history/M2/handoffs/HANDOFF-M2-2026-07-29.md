# Handoff: milestone M2

- **Date:** 2026-07-29（Asia/Shanghai）
- **Base branch/closeout source:**
  `master@7907cda05a56e1c3b85af2dd8faeb85a385154b7`
- **Remote-qualified product/control commit:**
  `7907cda05a56e1c3b85af2dd8faeb85a385154b7`
- **Milestone status:** closed

## Current state

M2 的六个 in-scope tickets（M2-T01～M2-T06）均为 `done`，release
dependency checks clear，runtime phases 为空，没有 open canonical root。
`workflow.py next --milestone M2 --json` 的 pre-close terminal action 为
`ready_to_close`，`done=6`；durable roadmap 状态现为 `closed`。

产品、控制面与 release 证据绑定 exact
`7907cda05a56e1c3b85af2dd8faeb85a385154b7`。本 handoff、ticket、roadmap、
vision、CI status、review-debt 与 test-budget baseline 的后续 closeout commit
是本地 docs/coordination-only 后代，不是新的 remotely qualified product SHA。

GitHub Actions
[run 30425476328 attempt 1](https://github.com/zzffu/ferrum2/actions/runs/30425476328)
由 exact `7907cda` 的一次授权 fast-forward push 触发。GitHub run/jobs REST
与登录态 raw job log 的只读复核确认 run 为 `push` event、
`completed/success`，且同一 SHA/run/attempt 的六项 required results 全部
`completed/success`：

| Result | Job ID |
|---|---:|
| `quality` | `90490943645` |
| `msrv` | `90490943661` |
| `platform / linux-musl` | `90490943702` |
| `platform / linux-gnu` | `90490943716` |
| `platform / windows-msvc` | `90490943726` |
| `interop` | `90490943768` |

`quality` raw log 中
`ipv4_ingress_ipv6_direct_target_round_trips_three_datagrams_and_reaps`
恰执行一次并为 `ok`；summary 为 `1 passed; 0 failed; 0 ignored`。产品 marker
恰出现一次，记录三 datagrams 且 payload/source/cleanup 均为 `PASS`；completion
marker 也恰出现一次，并绑定 exact SHA `7907cda`、run `30425476328`、attempt
`1`。

`interop` raw log 中 `provider_setup sing_box=0 shadowsocks_rust=0` 恰出现一次；
M1-INT-001～012 与 M2-UDP-INT-001～012 各有十二条 `status=PASS`。TCP 与 UDP
summary 都是 `12/12`、`cleanup=PASS`并绑定同一 SHA/run/attempt，最终
provider/qualification/cleanup status 全为零。

## Completed work

| Ticket | Delivered outcome | Reviewed candidate / relevant checkpoint | Durable state |
|---|---|---|---|
| M2-T01 | method-bound AES separate-header / ChaCha XChaCha UDP capability、non-resettable outbound counter、reviewed primitives | `c7ebe918e2d02664ec21fcfad85c301cbb6d3c01` | done |
| M2-T02 | packet/address semantics、8,129-value replay、current+old associations、roaming、generation binding | `4d1c65b4d9af03f008b51cae3b5f058ca1edea64` | done |
| M2-T03 | protocol-neutral datagram contract、bounded session/byte/queue/runtime ownership | `491954d8ea8fdf5faad17b0b360f353283d44898` | done |
| M2-T04 | same-port TCP+UDP composition、direct UDP、offline config、telemetry、local lifecycle | `980540bd439c438eb196cbc3096cbea0cda3fb4d`；port fixture repair `87c3c32c508222595cd78f442ce6091b5818e1ec` | done |
| M2-T05 | fixed TCP/UDP external qualification、provider continuation、causal EOF/cleanup proof | `0395d7dfb170ddc8c3328b2d939210d96c81266f`；qualification checkpoint `52d1610a127349e7a817a67c81c77e0383d20d1e` | done |
| M2-T06 | IPv4 SS UDP ingress → composed server → IPv6-only direct target focused row、exact Linux quality execution | `d1c12627632112826fe3dee884caf5facb291e48`；integrated/qualified `7907cda05a56e1c3b85af2dd8faeb85a385154b7` | done |

M2 exit criteria：

| Exit criterion | Primary evidence | Result |
|---|---|---|
| Three methods：UDP crypto/wire/address/bounds/negative behavior | T01/T02 parameterized fixtures；exact local full and hosted quality | PASS |
| Session/nonce/counter uniqueness and exhaustion | T01 repaired counter/session capability；`ARCH-M2-T01-001` targeted PASS | PASS |
| 8,129 replay and post-validation/reservation atomic commit | T02 repaired candidate；T04 request/response commit placement satisfies `QA-M2-T02-N01` | PASS |
| Authenticated routing、roaming、association/generation retention | T02 packet/session evidence and exact integration reviews | PASS |
| Bounded sessions/bytes/queues/idle/lifecycle | T03/T04 runtime, saturation, expiry, cancellation and cleanup evidence | PASS |
| Atomic composition、offline config、telemetry、local product、IPv6 direct target | T04 local matrix plus T06 exact hosted focused row and marker audit | PASS |
| Fixed 12-case UDP interop and cleanup | run `30425476328/1` exact `7907cda`：UDP `12/12` + cleanup PASS | PASS |
| M0/M1 regression、budget、MSRV、三平台和 full | exact local quick/full；same run six jobs；TCP `12/12` + cleanup PASS | PASS |

SPEC-0003 M2-AC-01～08 均有 TEST-0003 指定的 primary evidence。Product
Manager 与 Architect close review 均 `PASS_WITH_NOTES`，QA close review
`PASS`；没有新 blocker 或 major finding。

## Decisions and contracts

- `ADR-0020`：method-bound UDP envelope；AES separate header/session key，
  ChaCha direct-PSK XChaCha capability；secret/counter ownership留在crypto deep
  module，TCP compatibility 由 `TcpMethodProfile` 保持。
- `ADR-0021`：8,129-value per-direction replay window、authenticated session
  identity、current+old associations、validated roaming、generation-bound response；
  authentication、完整 semantics 和 reservation 之前不得 mutation。
- `ADR-0022`：固定 session/allocated-byte/depth-4 queue/idle limits；每 session
  一 socket/task owner；reservation-before-commit；server same-port TCP+UDP
  startup 为 transaction。
- `SPEC-0003` 和 `TEST-0003` 保持 Approved；closeout 没有修改 wire、crypto、
  replay、安全顺序、配置 schema、server listener 或 public API contract。
- v0 public client 仍只有 SOCKS5 TCP `CONNECT`。M2 protocol API 的 UDP
  证据不暗示 SOCKS5 `UDP ASSOCIATE`、IPv6 Shadowsocks listener 或其他 public
  UDP inbound。
- 外部 qualification 固定为三方法 × 两 references × 两 directions 的十二项
  UDP matrix；同一 run 继续执行十二项 TCP regression。
- 不新增 ADR。三位 close reviewer 均确认现有 ADR/spec 不矛盾。

## Validation evidence

Team Lead 在 exact product/control candidate 与 close mode 实际执行：

| Commit/worktree | Command | Exit/result |
|---|---|---:|
| `codex/integration/m2@7907cda` | `workflow.py run-validation quick --cwd .worktrees/_integration-m2` | 0；3/3 PASS |
| same | `workflow.py run-validation full --cwd .worktrees/_integration-m2` | 0；4/4 PASS |
| same | workflow-control unittest discovery | 0；75/75 PASS |
| same | qualification contract | 0；13/13 PASS |
| same | workspace policy | 0；17/17 PASS |
| same | UDP local process suite | 0；3 passed、1 ignored；IPv6 row未在Windows执行 |
| same | `workflow.py test-budget --gate ticket --base a4fa688... --json` | 0；delta `0/120`，allowance `120` |
| same | `workflow.py test-budget --gate milestone --base b804a01... --json` | 0；delta `3994/3475`，allowance `4114` |
| same | `workflow.py doctor`、`validate`、`git diff --check` | 0；仅既有 M0 soft warnings |
| run `30425476328` | GitHub run/jobs REST | 0；exact SHA/event/attempt，6/6 success |
| quality job `90490943645` | authenticated raw-log exact marker audit | PASS；row/product/completion counts均为1 |
| interop job `90490943768` | authenticated raw-log case/summary audit | PASS；TCP 12/12、UDP 12/12、cleanup PASS |
| `master@7907cda` + closeout working tree | `python -m unittest discover -s .agents/skills/milestone-workflow/tests -p 'test_*.py'` | 0；75/75 PASS |
| same | `workflow.py doctor`、`validate`、`review-state M2-T06`、`gate-check M2-T06 release` | 0；review PASS、release clear |
| same | `workflow.py next --milestone M2 --json` | 0；`ready_to_close`、done=6、无open root/phase |
| same | `workflow.py test-budget --gate report --tool builtin --json` | 0；`11714/19234/1.641967` |
| same | `git diff --check` | 0 |

Close reviewer QA 还独立重跑 `doctor`、`validate`、T06 review/release、scheduler
和 milestone budget，均 exit 0。配置的 reviewer profiles 与 observable actual
profile 分开记录如下。

Test-budget ratchet：

- accepted source：`7907cda05a56e1c3b85af2dd8faeb85a385154b7`；
- code `11714`；
- tests `19234`；
- files `66`；
- ratio `1.641967`；
- previous baseline ratio `2.041321`；
- milestone required ratio `1.991321`；
- planning-base delta `+3994/+3475`，allowance `4114`；
- milestone verdict `PASS`。

三个 close reviewer 接受 exact candidate 后，
`codex/test-budget-baseline.json` 通过以下命令推进到 accepted source：

```powershell
$env:PYTHONUTF8='1'
python .agents/skills/milestone-workflow/scripts/workflow.py `
  test-budget --gate report --tool builtin --write-baseline --json
```

命令 exit 0 并报告 `baseline_written=true`。Ratio `1.642` 高于 warning ratio
`0.850` 是 advisory；ratchet gate 已通过，不得以无意义 production code 改善
比例。

## Existing branches and worktrees

closeout 没有删除 branch 或 worktree：

| Worktree | Branch/HEAD | State before closeout commit |
|---|---|---|
| repository root | `master@7907cda` | closeout docs/baseline working tree |
| `.worktrees/_integration-m2` | `codex/integration/m2@7907cda` | clean |
| `.worktrees/m2-t01` | `codex/ticket/m2-t01@c7ebe91` | clean |
| `.worktrees/m2-t02` | `codex/ticket/m2-t02@4d1c65b` | clean |
| `.worktrees/m2-t03` | `codex/ticket/m2-t03@491954d` | clean |
| `.worktrees/m2-t04` | `codex/ticket/m2-t04@c6ade6d` | clean |
| `.worktrees/m2-t05` | `codex/ticket/m2-t05@bc589ee` | clean |
| `.worktrees/m2-t06` | `codex/ticket/m2-t06@d1c1262` | clean |
| `.worktrees/m2-t05-hosted-repair` | `codex/repair/m2-t05-hosted-ordering@0395d7d` | clean |
| `.worktrees/m2-integration-port-repair` | `codex/repair/m2-integration-port-reservation@87c3c32` | clean |
| `.worktrees/m2-t05-control` | `codex/workflow/m2-t05-targeted-findings@dff012a` | clean |
| `.worktrees/m2-hosted-release-control` | `codex/workflow/m2-hosted-release-review-lifecycle@6bc85d6` | clean |
| `.worktrees/m2-root-cycle-control` | `codex/workflow/m2-root-cycle-targeted-pass@9528679` | clean |

Cargo `target/`、runner logs、downloaded raw logs、packet captures 和 benchmark
outputs 是 generated artifacts，不提交。不要 forced-clean retained worktrees；
删除 branch/worktree 需要另行明确授权。

## Active execution phases and ownership

- Runtime phases：`{}`。
- Active ownership leases：none。
- Active Engineer/reviewer work：none。
- close mode 没有 implementation wave；scheduler active/frontier均为空。
- durable roadmap state：`closed`；helper 的 pre-close terminal action 保留为
  `ready_to_close`，因为 helper 没有单独的 milestone-close mutation。

## Review rounds and finding history

| Scope | Review evidence |
|---|---|
| M2-T01 | QA full `PASS`；Architect full `BLOCK` on `ARCH-M2-T01-001`；one repair；Architect targeted `PASS` |
| M2-T02 | QA full `PASS_WITH_NOTES`；Architect full `BLOCK` on `ARCH-M2-T02-001`；one repair；Architect targeted `PASS` |
| M2-T03 | Architect/QA full `BLOCK` on `ARCH-M2-T03-001` / `QA-M2-T03-001`；one combined repair；both targeted `PASS` |
| M2-T04 | Architect/QA full `BLOCK`；one substantive repair；both targeted `PASS_WITH_NOTES`；one later mechanical fixture repair |
| M2-T05 | full/targeted/superseding and hosted-root histories retained；authorized repairs/control amendments converged to exact `0395d7d` and qualification checkpoint `52d1610` |
| M2-T06 | Architect/QA full `BLOCK` at `f4fabe8` on `ARCH-M2-CLOSE-001` / `QA-M2-CLOSE-001`；one evidence repair `d1c1262`；both targeted `PASS` |
| M2 close | Product Manager `PASS_WITH_NOTES`；Architect `PASS_WITH_NOTES`；QA `PASS`；no blocker/major |

T06 control root `M2-CLOSE-REVIEW-CONTROL-001` was resolved by authorized
`dd646ae` plus one bounded repair `9528679` after Architect finding
`ARCH-WF-ROOT-CYCLE-002`。Ordinary full-BLOCK → one repair → targeted-PASS is now
terminal on one common non-empty SHA；if any required reviewer targeted-ESCALATEs，
every required reviewer still needs an authorized superseding record。Workflow
`75/75`、Architect targeted PASS 和 QA repair-delta PASS were recorded；no product
contract or remote state changed。

## Canonical blockers and derivatives

Current open roots：none。

Close root `M2-CLOSE-IPV6-001`：

- class/risk/gate：`test_evidence` / `high` / release；
- derivatives：`ARCH-M2-CLOSE-001`、`QA-M2-CLOSE-001`；
- one authorized evidence repair：`d1c1262`；
- full reviews at `f4fabe8` were BLOCK，both targeted reviews at `d1c1262`
  were PASS；
- final resolution：exact `7907cda` run `30425476328/1` completed all six jobs；
  focused IPv6 row/product/completion markers each appeared exactly once；TCP/UDP
  both 12/12 + cleanup PASS；
- status：resolved。

Historical T05 qualification/root-cycle/hosted-ordering roots、T04 port-fixture
root、T01/T02/T03/T04 review roots 和 workflow-control derivatives均为
`resolved`。Failed or superseded evidence remains in history；closeout没有覆写、
删除或拼接这些 records。

## Active authorization scopes

Active scopes：none。

Close-path scopes：

| Scope | Kind/action | Final state/use |
|---|---|---|
| `m2-20260729-close-ipv6-evidence-repair-a1` | local / `close_evidence_repair` | revoked；1/1 |
| `m2-20260729-close-ipv6-local-repair-a1` | local / `local_repair` | revoked；1/1 |
| `m2-20260729-root-cycle-targeted-pass-control-a1` | local / `workflow_control_amendment` | revoked；1/1 |
| `m2-20260729-close-ipv6-review-architect-a1` | local / obsolete review override | revoked；0/1 |
| `m2-20260729-close-ipv6-review-qa-a1` | local / obsolete review override | revoked；0/1 |
| `m2-20260729-remote-qualification-7907cda-a1` | remote / `remote_qualification` | revoked；1/1 |

Earlier T05 repair/review/control scopes and exact remote scopes for
`a168b89`、`52d1610` are also revoked/exhausted in the runtime ledger。The final
remote scope was bound only to
`refs/heads/codex/integration/m2@7907cda05a56e1c3b85af2dd8faeb85a385154b7`。
It does not authorize a second push、rerun、remote `master`、PR、tag、release、
publication、ref deletion or any other remote/destructive action。Close mode created
or consumed no additional authorization。

## Agent and gate provenance

Configured profiles：

| Gate | Requested/configured profile | Observable actual profile | Verdict |
|---|---|---|---|
| Product closeout | `product_manager / gpt-5.6-sol / medium` | unverified | PASS_WITH_NOTES |
| Architecture closeout | `architect / gpt-5.6-sol / high` | unverified | PASS_WITH_NOTES |
| QA closeout | `qa / gpt-5.6-sol / medium` | unverified | PASS |
| T06 implementation | `engineer / gpt-5.6-sol / high` | unverified | DONE |
| Root-cycle control | `engineer / gpt-5.6-sol / high` | unverified | DONE |

Collaboration runtime没有向Team Lead或reviewer暴露actual serving model/reasoning
telemetry，因此不能把configured profile声称为observed actual。Close reviewers
只读；没有 tracked edit、ledger mutation、merge、push、publish 或 subagent。

## Preserved non-PASS development evidence

- First hosted M2 run `30408245840/1` at superseded `a168b89` passed UDP 12/12
  but TCP only 9/12；three SingBox reference-client cases exposed
  `ApplicationCleanEof` before target EOF/shutdown evidence。该 run保持FAIL，未
  rerun/splice。
- T05 hosted-ordering、budget 和 review-control work required authorized bounded
  repairs；full/targeted/ESCALATE/superseding history remains immutable。
- Two concurrent full gates on `6fd07a0` produced one PASS and one lifecycle-witness
  timeout；serialized isolation passed。A later serialized full exposed separate
  Windows `config_cli` UDP-bind `WSAEACCES`，resolved by mechanical `87c3c32`
  after 256/256 focused repetitions and exact full 4/4。
- T06 development retained initial formatting、missing-binary setup and interim
  budget failures at larger deltas；the final candidate passed all named gates。
  Windows local IPv6 row remained **NOT EXECUTED** until the hosted Linux job。
- Initial root-cycle control candidate `dd646ae` received Architect BLOCK
  `ARCH-WF-ROOT-CYCLE-002`；the one repair `9528679` passed targeted review。
  An intermediate green attempt failed because its fixture contradicted the unchanged
  strict validator；the corrected suite passed 75/75。
- Initial Windows budget invocations without UTF-8 mode hit GBK decode failures and
  produced no valid budget verdict；only identical `PYTHONUTF8=1` reruns are credited。
- The first `set-status M2-T06 done` call incorrectly supplied legacy transient
  metadata and exited 1；the corrected helper call exited 0, changed only durable
  status `ready` → `done`, and cleared the runtime phase。
- GitHub CLI was unavailable and anonymous raw job-log API requests returned 403；
  no rerun or write fallback was attempted。Run/jobs REST and authenticated
  read-only raw-log inspection supplied the final evidence。

## Known risks and debt

- `M2-INT-QA-001`：resource-heavy full gates must remain serialized。Reopen only
  if a serialized authoritative/hosted gate reproduces readiness failure or evidence
  points to shipped listener ownership/lifecycle behavior。
- `ARCH-M2-T05-HOSTED-N02`：100 ms focused scheduler bound remains advisory after
  one loaded timeout and a later diagnostic 100/100 plus successful hosted evidence。
- Windows T04/T06 local history remains honestly **NOT EXECUTED** for the IPv6-only
  row；Linux exact-SHA quality evidence satisfies release，不改写本机结果。
- test/code ratio `1.641967` remains above warning threshold；milestone ratchet
  passed。Do not add meaningless production code to improve it。
- No unresolved M2 P0/P1 product blocker remains。Complete v0仍需M3/M4。

## Deferred work

- M3：freeze operator-facing TOML/CLI/error contracts，stable tracing/Prometheus
  schema/redaction，TCP/UDP cancellation/timeout/listener/graceful-shutdown soak，
  and locked Linux GNU/musl/Windows release artifacts with offline config smoke。
- M4：reproducible loopback throughput ≥ comparable shadowsocks-rust 90%、
  10,000 idle TCP stability，and same-commit integrated v0 qualification。
- v0 non-goals continue to include SIP023/multi-user、SOCKS5 UDP ASSOCIATE、
  routing、DNS proxy/cache、multi-upstream/load balancing、proxy chaining、
  hot reload、management API、reduced-round ChaCha、custom executor and
  `io_uring`。

## Recovery instructions

Audit M2 closeout：

```powershell
python .agents/skills/milestone-workflow/scripts/workflow.py doctor
python .agents/skills/milestone-workflow/scripts/workflow.py validate
python .agents/skills/milestone-workflow/scripts/workflow.py status --milestone M2
python .agents/skills/milestone-workflow/scripts/workflow.py worktree-list
python .agents/skills/milestone-workflow/scripts/workflow.py state --milestone M2
python .agents/skills/milestone-workflow/scripts/workflow.py next `
  --milestone M2 --json
```

Do not recreate/reset retained worktrees，reuse exhausted authorization，splice
different SHA/run/attempt evidence，rewrite Windows NOT EXECUTED as local IPv6 PASS，
or push the docs-only closeout commit without new explicit authority。

## Next recommended action

After the local M2 closeout commit，the next entry is：

```text
$milestone-workflow
mode: plan
milestone: M3
goal: Freeze and validate the two binaries' operator-facing configuration, CLI,
observability, lifecycle, and three-target release-artifact contracts on top of the
closed TCP/UDP protocol surface, without adding M4 performance claims or new v0
features.
```

This entry only starts M3 contract planning；it does not automatically implement、
push、open a PR、tag、release or publish。
