# Handoff: milestone M0

- **Date:** 2026-07-28（Asia/Shanghai）
- **Base branch/closeout source:** `master@b0717d23db9fe9f13ac58ab43ec159274942ef5b`
- **Product/release commit:** `8318ef106d6cd4e029bd3b02aa64125fabdda462`
- **Milestone status:** closed

## Current state

M0 的十个 in-scope tickets（M0-T01～M0-T10）均为 `done`，release dependency
checks全部clear。产品与workflow资格证据绑定到exact integration
`8318ef106d6cd4e029bd3b02aa64125fabdda462`；closeout source
`b0717d23db9fe9f13ac58ab43ec159274942ef5b`是其仅修改roadmap、CI status和T10
evidence的docs-only后代，不是新的product release SHA。

GitHub Actions
[run 30331336772 attempt 1](https://github.com/zzffu/ferrum2/actions/runs/30331336772/attempts/1)
由exact `8318ef1`的授权push触发，完整workflow为success。public run page和
read-only Jobs API显示六项required results均为`completed/success`：

| Result | Job ID |
|---|---:|
| `quality` | `90186953479` |
| `msrv` | `90186953396` |
| `platform / windows-msvc` | `90186953489` |
| `platform / linux-gnu` | `90186953464` |
| `platform / linux-musl` | `90186953441` |
| `interop` | `90186953484` |

旧run `30301746374`和`30322690937`继续保持失败历史；其局部success没有与最终run
拼接或追认。匿名job-log下载返回HTTP 403，但run/job identity、SHA、attempt和
conclusion可公开复核；reviewed workflow的fail-closed aggregation仍要求两个
provider setup成功、qualification exit 0且四项interop为4/4。

## Completed work

| Ticket | Delivered outcome | Durable state |
|---|---|---|
| M0-T01 | locked workspace、MSRV/license/unsafe policy、core contracts | done |
| M0-T02 | secret owners、KDF/AEAD KAT、entropy/clock/nonce boundaries | done |
| M0-T03 | SIP022 AES-128 TCP framing、binding、replay、detection、duplex state | done |
| M0-T04 | typed TOML validation、redacted tracing、bounded metrics | done |
| M0-T05 | SOCKS5 no-auth IPv4 `CONNECT` inbound | done |
| M0-T06 | direct outbound、bounded relay、timeouts、ownership与shutdown | done |
| M0-T07 | 两个binary composition、offline config、local E2E、100-cycle lifecycle | done |
| M0-T08 | historical selected interop/platform/CI profile and evidence repairs | done |
| M0-T09 | Cargo-managed、`test = false` hosted qualification seam与4/4 aggregation | done |
| M0-T10 | thin quality/MSRV/platform/interop hosted outcome profile | done |

M0 exit criteria全部有直接证据：

| Exit criterion | Result |
|---|---|
| Workspace/toolchain/license/unsafe/module topology | PASS |
| Offline config and pre-listener semantic failure | PASS |
| SOCKS5 → SIP022 AES-128 → direct TCP product path | PASS |
| KAT、authentication ordering、bounds、binding、timestamp/replay | PASS |
| Backpressure、half-close、timeout、cancellation、100-cycle cleanup | PASS |
| Secret-safe tracing and bounded-cardinality metrics | PASS |
| sing-box/shadowsocks-rust四项双向TCP interoperability | PASS |
| local quick/full、MSRV、Windows/GNU/musl、exact-SHA hosted workflow | PASS |

## Decisions and contracts

- ADRs: `ADR-0001`～`ADR-0017`均为 Accepted。
- Spec: `docs/specs/SPEC-0001-m0-aes128-tcp-vertical-slice.md`及
  ADR-0010～0017 amendments为 Approved。
- Test plan: `docs/test-plans/TEST-0001-m0-aes128-tcp-vertical-slice.md`及
  amendments为 Approved。
- Tickets: `docs/tickets/M0-T01-*.md`～`M0-T10-*.md`均为 `done`。
- ADR-0017只替换qualification seam、job/filter/linker/scope self-audit mechanics；
  trigger/security/exact-SHA、platform、reference、failure和4/4 outcome invariants
  保持不变。
- 没有为closeout新建ADR或修改wire、product API、security ordering、dependency
  graph、toolchain、reference pins或release behavior。

## Validation evidence

Team Lead在本次closeout实际执行：

| Commit/worktree | Command | Exit/result |
|---|---|---:|
| `master@b0717d2` | `python --version` | 0；Python 3.11.9 |
| same | `python .agents/skills/milestone-workflow/scripts/workflow.py doctor` | 0 |
| same | `python .agents/skills/milestone-workflow/scripts/workflow.py validate` | 0 |
| same | `workflow.py status`、`state --milestone M0`、`worktree-list` | 0 |
| same | `workflow.py next --milestone M0 --json` | 0；pre-close `ready_to_close` |
| same | M0-T01～M0-T10各自`gate-check <ticket> release --json` | 0；10/10 clear |
| `codex/integration/m0@8318ef1` | `cargo fmt --all -- --check` | 0 |
| same | `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | 0 |
| same | `cargo test --workspace --all-features --locked` | 0 |
| same | `cargo doc --workspace --all-features --no-deps --locked` | 0 |
| closeout docs working tree | `workflow.py validate` | 0 |
| closeout docs working tree | `git diff --check` | 0 |

Exact `8318ef1`已有并在closeout重新确认authoritative quick 3/3、full 4/4与
workspace binary build证据；all-feature test rerun包括real-process local E2E、
exact 100-cycle lifecycle、qualification pure-state、workspace policy、crypto、
replay、negative、binding、backpressure、shutdown和observability suites。
Cargo-managed `m0-qualification` external entry没有在本地执行；真实reference
process/network cases只由上述hosted `interop` result证明。

完整closeout文档加入后，workflow unit tests为53/53 PASS，
`workflow.py validate`与`git diff --check`均exit 0，required-section scan完整且
stale-current-state scan无finding。exact `8318ef1`已有quick 3/3证据；本次closeout
独立重跑的是full 4/4。

## Existing branches and worktrees

没有branch或worktree在closeout中被删除。主要durable pointers：

| Worktree | Branch/HEAD | State before closeout commit |
|---|---|---|
| repository root | `master@b0717d2` | closeout docs working tree |
| `.worktrees/_integration-m0` | `codex/integration/m0@8318ef1` | tracked clean；ignored `target/` generated by validation |
| `.worktrees/m0-t01` | `codex/ticket/m0-t01@edaee3d` | clean |
| `.worktrees/m0-t02` | `codex/ticket/m0-t02@6a05803` | clean |
| `.worktrees/m0-t03` | `codex/ticket/m0-t03@3a9114d` | clean |
| `.worktrees/m0-t04` | `codex/ticket/m0-t04@8d18d17` | clean |
| `.worktrees/m0-t05` | `codex/ticket/m0-t05@d03e006` | clean |
| `.worktrees/m0-t06` | `codex/ticket/m0-t06@721ed02` | clean |
| `.worktrees/m0-t07` | `codex/ticket/m0-t07@a9b0a56` | clean |
| `.worktrees/m0-t08` | `codex/ticket/m0-t08@5accd02` | clean |
| `.worktrees/m0-t09` | `codex/ticket/m0-t09@cb193a1` | clean |
| `.worktrees/m0-t10` | `codex/ticket/m0-t10@5ad65d6` | only untracked generated `target/` |

Retained repair/plan/diagnostic branches：

- `codex/contract/m0-invariant-evidence@a1a3c7b`
- `codex/plan/m0-ci-convergence@dc8e98d`
- `codex/diagnose/m0-sing-response@14343d2`
- `codex/diagnose/m0-t08-lifecycle-flake@5accd02`
- `codex/repair/m0-t02-adr0016-provenance@d89e46e`
- `codex/repair/m0-t03-client-open-phase@8f0d1e0`
- `codex/repair/m0-t03-provenance-eol@1f76597`
- `codex/repair/m0-t06-relay-outcome@0ef7969`
- `codex/repair/m0-t07-hosted-rebind@f8d8912`
- `codex/repair/m0-t07-msrv-let-chain@50bf0b7`
- `codex/repair/m0-t07-readiness-ownership@6139544`
- `codex/repair/m0-t08-final-closure@b8baba2`

Detached review worktrees仍保留：`_review-m0-final-architect@49c7000`、
`_review-m0-final-candidate@49c7000`、`_review-m0-final-qa@49c7000`、
`_review-m0-t08-b8baba2@fe90833`与`_review-m0-t08-dbcb6d2@b03762f`。
其中若干以及T02/T07/T08 repair worktrees只含untracked `target/`；它们是generated
artifacts，不是active product changes。不要用forced cleanup；后续如需删除，先以
`workflow.py worktree-list`确认tracked clean并单独授权。

## Active execution phases and ownership

- Runtime ledger phases：`{}`。
- Active ownership leases：none。
- Active Engineer/reviewer work：none。
- Closeout没有implementation wave；strategy仍为configured `drain`，waves=0。

## Canonical blockers and derivatives

Closeout开始前所有历史product/review/integration/release roots均为resolved：

- `M0-T02-ADR0016-PROVENANCE-PIN`
- `M0-T07-HOSTED-REBIND`
- T08 roots：`M0-T08-CONTROL-DOC-ALLOWLIST`、
  `M0-T08-CONTROL-PLANE-SCOPE`、`M0-T08-HOSTED-EXACT-FILTERS`、
  `M0-T08-HOSTED-LINUX-LINKER`、`M0-T08-HOSTED-WINDOWS-LINKER`、
  `M0-T08-INTEROP-REBIND-POLICY`
- T08 resolved derivatives：`M0-T08-HOSTED-GENERAL-FAILURE-FILTER`、
  `M0-T08-HOSTED-LINUX-TIMEOUT-KIND`、`M0-T08-HOSTED-SCOPE-AUDIT-RG`、
  `M0-T08-HOSTED-WINDOWS-LINKER-1100`
- `M0-T09-PROVIDER-STATUS-AGGREGATION`
- T10 root `M0-T10-INTEROP-CHECKOUT-POLLUTION`及其
  `M0-T10-CLEAN-GUARD-EXIT-STATUS`、`M0-T10-GNU-GLIBC-EVIDENCE`、
  `M0-T10-PROVIDER-SETUP-DEADLINE`、`M0-T10-PROVIDER-SETUP-STATUS-MASKED`、
  `M0-T10-REFERENCE-STAGING-MISSING` derivatives
- root `M0-CI-CONVERGENCE`及`M0-T10-MSRV-FRESH-BINARIES` derivative
- `M0-BASE-WORKTREE-DIRTY`
- `M0-CLOSEOUT-FULL-BINARY-PRECONDITION`

本次close gate记录并解决了一个low-risk `test_evidence` root：
`M0-CLOSEOUT-STALE-EVIDENCE-STATUS`。其derivatives是vision/roadmap、
SPEC/TEST、T07/T08/T09中把已解决工作写成current planned/blocked状态的文档。
closeout diff修复其事实原因并加入durable handoff；Product、Architect、QA最终均
PASS，workflow unit 53/53、validation与diff-check通过，Team Lead已在runtime
ledger显式resolve。当前没有open canonical root。

Closeout本身没有product repair round。ledger保留12个历史repair events：
M0-T02=1、M0-T07=1、M0-T08=6、M0-T09=1、M0-T10=3；没有
root-bound repair-budget override allowance。

## Active authorization scopes

Active scopes：none。

| Scope | Kind | Final state/use |
|---|---|---|
| `m0-resume-20260728-local-narrow-repairs` | local | revoked；0 uses |
| `m0-resume-20260728-local-narrow-repairs-v2` | local | revoked；7 uses |
| `m0-20260728-exact-skill-optimization-audit-exclusion` | local | exhausted/revoked；1/1 |
| `m0-20260728-final-integration-push-5969bfd` | remote | exhausted/revoked；1/1 |
| `m0-20260728-final-integration-push-8318ef1` | remote | exhausted/revoked；1/1 |

两个remote scopes只分别绑定
`refs/heads/codex/integration/m0@5969bfdafea9056feb179e0a8454dd5dc7fe5bce`
和
`refs/heads/codex/integration/m0@8318ef106d6cd4e029bd3b02aa64125fabdda462`。
它们不授权master push、第二次push、rerun、PR、branch protection、tag、release、
branch/worktree deletion或其他remote/destructive action。close mode没有消费或创建
任何authorization。

## Agent and gate provenance

`workflow.py doctor`确认configured profiles：

| Gate | Requested/configured profile | Observable runtime profile | Current verdict |
|---|---|---|---|
| Product closeout | `product_manager / gpt-5.6-sol / max` | unverified | PASS |
| Architecture closeout | `architect / gpt-5.6-sol / max` | unverified | PASS on `8318ef1` + docs-only closeout source |
| QA closeout | `qa / gpt-5.6-sol / high` | unverified | PASS |

角色本身由exact named `agent_type`启动；collaboration runtime没有暴露实际model/
reasoning telemetry，因此不能把configured profile声称为observed actual。三个角色均
只读，没有tracked edit、merge、push或publish。

## Known risks and debt

- Anonymous GitHub job logs不可见；public run/jobs conclusions和reviewed
  fail-closed workflow足以证明M0结果，但raw runner/tool/artifact detail不在本地归档。
- GitHub-hosted image会漂移；M0只声明artifact/config/detection smoke，长期平台
  qualification属于M3。
- Retained worktrees含ignored/untracked `target/`；没有active lease或tracked partial
  work，且未执行destructive cleanup。
- Windows本地可复现helper入口是`python`；文档中的泛化`python3` portability改进是
  control-plane debt，不阻塞M0 product。
- 未定义throughput baseline、10,000-idle threshold、soak duration或resource
  statistics；M0不作performance claim。
- M0没有unresolved P0/P1 product blocker。

## Deferred work

- M1：AES-256-GCM、ChaCha20-Poly1305、IPv6/domain TCP behavior及
  `3 methods × 2 references × 2 directions = 12` TCP interop。
- M2：三个方法的SIP022 UDP、per-direction replay windows、bounded session/resource
  lifecycle及12项UDP interop；不暗示公开UDP inbound。
- M3：operator-facing contract、长期lifecycle/soak及三目标完整平台资格。
- M4：可复现throughput ≥ comparable shadowsocks-rust 90%、10,000 idle稳定性与
  same-commit v0 release qualification。
- v0 non-goals继续包括SIP023/multi-user、SOCKS5 UDP ASSOCIATE、routing、DNS、
  multi-upstream/load balancing、proxy chaining、hot reload、management API、
  custom executor与`io_uring`。

## Recovery instructions

Closeout中断或需要审计时：

```powershell
python .agents/skills/milestone-workflow/scripts/workflow.py doctor
python .agents/skills/milestone-workflow/scripts/workflow.py validate
python .agents/skills/milestone-workflow/scripts/workflow.py status
python .agents/skills/milestone-workflow/scripts/workflow.py worktree-list
python .agents/skills/milestone-workflow/scripts/workflow.py state --milestone M0
python .agents/skills/milestone-workflow/scripts/workflow.py next --milestone M0 --json
```

不要recreate/reset现有worktree，不要把旧failed run局部结果拼接为PASS，也不要复用
已耗尽remote scope。若closeout root仍open，先核实当前docs diff与三方gate verdict，
再使用`workflow.py resolve-blocker`；不要仅为改变scheduler output而关闭root。

## Next recommended action

M0最终doc re-review与root resolution已经完成。closeout commit后下一入口是：

```text
$milestone-workflow
mode: plan
milestone: M1
goal: Extend the proven M0 TCP slice to all three methods and complete the 12-case TCP interoperability matrix.
```

该入口只开始M1 contract planning；不自动push、开PR、发布release或实施M1。
