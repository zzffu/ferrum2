# Installer-managed Codex Configuration

The role definitions and Codex configuration in this subtree are control-plane
files. Product milestones may read them but must not edit them. Preserve project
model/reasoning preferences through the installer rather than changing role TOML
inside an Engineer worktree.

A missing workflow capability is workflow debt, not product code. Record it and stop
for an explicit workflow-maintenance decision.
