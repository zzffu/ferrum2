# Performance Evidence Fixture Guidelines

These JSON documents are minimal versioned contracts for external manifests and release evidence. Keep
them deterministic, complete, and small enough for direct review.

When a schema version changes, update the verifier and its positive and negative tests together; do
not silently accept the previous shape.
