# UDP worker controller tests

These tests cover the closed trial plan, ABBA order, source binding, schema
rejections, and provisional summary reduction. They use generated JSON and
fake process execution only; ordinary test discovery must never start a
performance workload.
