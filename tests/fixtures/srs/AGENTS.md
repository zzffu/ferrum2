# SRS Fixture Guidelines

These `.srs` files are small reviewed routing-rule samples, not production rule databases. Preserve
their documented origin and use them to test parsing and materialization contracts with bounded input.

If a sample is regenerated, update `README.md` with provenance and verify that its intended rule
categories remain represented.
