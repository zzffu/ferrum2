# SIP022 Fixture Guidelines

These files define versioned SIP022 protocol vectors and their deterministic Rust generators. Keep
TCP and UDP cases explicit, preserve `PROVENANCE.toml`, and review generated byte changes as protocol
changes rather than incidental snapshots.

Generators must produce identical output from fixed inputs and must not depend on the product code
being tested.
