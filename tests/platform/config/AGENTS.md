# Platform Configuration Fixture Guidelines

These TOML files are minimal client/server inputs for platform qualification script contracts. Keep
valid and invalid cases paired and focused on the platform workflow's observable validation boundary.

Do not add environment-specific credentials, host addresses, or compatibility-only fields.
